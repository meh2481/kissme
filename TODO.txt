Long-term:
- Clipboard support
- Undo/Redo
- Drag-and-drop support
- Drag multiple items
- Support for lots of common media formats: wma, m4a, mp3, flac, opus, wav, etc
- Save layout on exit and return to exactly the same state
- Save song location on exit to start from exactly the same point
- Support for common playlist types (See http://en.wikipedia.org/wiki/Playlist#Types_of_playlist_files); import (export?).
- Gapless playback? (See http://en.wikipedia.org/wiki/Gapless_playback)
- Unicode support
- Start up in seconds. Load files/tags as needed rather than up-front
- Save last window size on startup
- Progress bar when adding a bajillion files recursively with "Import Folder"

Short-term:
- Update volume when songs play based on volume control (not just when control is adjusted)

